const id = 123 //Can't update value
let emailId = "pratham@gmail.com" //value can be changed
var pasword = "*****" // Prefer not to use because of scope issue 
myName = "Pratham" // It is possible to declare without providing variable but never use it
let state

console.log(id)

console.table([id, emailId, pasword, myName, state])

